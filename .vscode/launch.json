{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Vercel AI MCP Dev Server",
      "type": "node-terminal",
      "request": "launch",
      "command": "vercel dev",
      "cwd": "${workspaceFolder}/mcp"
    }
  ]
}